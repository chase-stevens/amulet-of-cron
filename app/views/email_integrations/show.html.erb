<%= content_for :title, "Email Integrations ##{@email_integration.id}" %>
<%= turbo_stream_from @email_integration %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Integrations", integrations_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Email Integration #<%= @email_integration.id %>
    </h1>
    <%= link_to t("edit"), edit_email_integration_path(@email_integration), class: "btn btn-white" %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render @email_integration %>
  </div>
</div>
